<nav class="navbar navbar-expand-lg navbar-light bg-light">
  <div class="container-fluid">
    <a class="navbar-brand" href="{{ path('app_home') }}">Forum</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
          {% if is_granted('ROLE_ADMIN') %}
            <a class="navbar-brand text-danger" href="{{ path('app_profile') }}">{{ app.user.username }}</a>
          {% else %}
            <a class="navbar-brand text-primary" href="{{ path('app_profile') }}">{{ app.user.username }}</a>
          {% endif %}
          <a class="nav-link" href="{{ path('app_logout') }}">Logout</a>
          <a class="nav-link" href="{{ path('app_members') }}">Members</a>
        {% else %}
          <a class="nav-link" href="{{ path('app_login') }}">Login</a>
          <a class="nav-link" href="{{ path('app_register') }}">Register</a>
        {% endif %}
      </div>
    </div> 
  </div>
</nav>
